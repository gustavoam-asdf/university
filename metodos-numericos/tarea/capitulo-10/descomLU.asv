function [a, b, R] = descomLU(a, b, tol)
n = length(a);
er = 0;
o = zeros(n);
s = zeros(n);
[a, o, s, er] = descom(a, tol, o, s, er);
disp(er
if er == -1 
  [a, b, R] = substitute(a, b);
end
disp(a)
disp(b)
disp(R)